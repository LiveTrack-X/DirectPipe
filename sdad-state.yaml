version: 2
updated: 2026-07-27
scale: full
execution_scope: packet

active_spec: SPEC/SPEC-COMPLETE.md
active_packet:
  id: DP-421-RELEASE-0001
  objective: Integrate and publish the software-verified DirectPipe v4.2.1 release through exact-tag CI.
  status: in_progress

validation_for: DP-421-RELEASE-0001
owner_gates:
  - release_publication_authorized_by_OD-2026-07-27-0001
  - incompatible_ipc_api_or_streamdeck_change_requires_owner_decision

validation:
  - command: 'npm audit --audit-level=high && npm audit --omit=dev --audit-level=high'
    proves: Stream Deck development and production dependency graphs contain no known high-severity vulnerability.
  - command: 'npm run icons && npm test && npm run build && npm run validate'
    proves: The updated Stream Deck toolchain generates icons, passes 5 tests, builds, and validates the package.
  - command: C:/Program Files/Git/bin/bash.exe tools/pre-release-test.sh --version-only
    proves: The integrated release source and release notes are internally aligned to 4.2.1.
  - command: python tools/check_text_integrity.py --repo-root .
    proves: The integrated repository text and documentation remain decodable and safe to process.
  - command: git diff --check
    proves: The integrated main release source has no patch whitespace errors.
  - command: python C:/Users/livet/.codex/tmp/sdad-doctor-v3.2.2/scripts/sdad.py doctor . --require-version 3.2.2 --strict
    proves: The release packet, owner decision, ledgers, gates, and validation contract are structurally coherent.
  - command: gh run view RUN_ID --repo LiveTrack-X/DirectPipe
    proves: The exact-tag Windows, macOS, Linux, Stream Deck, and publish jobs all succeeded.
  - command: gh release view v4.2.1 --repo LiveTrack-X/DirectPipe
    proves: The public latest release contains the complete expected artifact set.

routed_docs:
  - SPEC/SPEC-COMPLETE.md
  - SPEC/DP-421-RELEASE-0001.md
  - SPEC/DP-421-0001.md
  - docs/owner-decisions.md
  - docs/TODO-Open-Items.md
  - review-findings.md
  - docs/implementation-notes.md
  - docs/Repository-Operating-Rules.md
